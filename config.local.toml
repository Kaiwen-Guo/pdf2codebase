[llm]
llm_provider = "none"
model_name = ""
api_key_env = ""
temperature = 0.0
max_tokens = 8192
use_llm_for_testplan = false
use_llm_for_code = false
use_llm_for_tests = false

[storage]
database_path = "verifaix.local.db"
artifacts_dir = "artifacts"

[extraction]
use_ocr = true
ocr_mode = "auto"
min_text_chars = 200
ocr_dpi = 200
use_vision_for_images = false
vision_mode = "auto"
max_vision_pages = 4
vision_dpi = 150

[runtime]
module_name = "generated_module"
fallback_to_deterministic = true
